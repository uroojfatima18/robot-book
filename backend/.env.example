# Database (Neon PostgreSQL)
DATABASE_URL="postgresql://user:pass@ep-xxx.us-east-1.aws.neon.tech/neondb?sslmode=require"

# Better Auth
BETTER_AUTH_SECRET="your-32-character-secret-here-min"
BETTER_AUTH_URL="http://localhost:3000"

# Vector Database (Qdrant Cloud)
QDRANT_URL="https://xxx.us-east-1.aws.cloud.qdrant.io:6333"
QDRANT_API_KEY="your-qdrant-api-key"

# OpenAI (for embeddings only)
OPENAI_API_KEY="sk-..."

# Google Gemini (for chat completions via OpenAI-compatible endpoint)
# The application uses OpenAI SDK configured to point to Gemini's endpoint
# This provides a standardized API interface while using Gemini models
GEMINI_API_KEY="your-gemini-api-key"

# Email (Resend)
RESEND_API_KEY="re_..."
EMAIL_FROM="noreply@yourdomain.com"
